# Game Textures

This folder contains textures used by exported models.

For the first asset pipeline milestone, use simple diffuse/albedo textures only.

Later milestones may add:

- normal maps
- roughness maps
- metalness maps
- ambient occlusion maps
- full PBR material support